{
  "name": "@acme/root",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "build": "FORCE_COLOR=1 turbo run build --filter='./packages/*' --filter='./apps/*'"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@changesets/config": "^3.1.4",
    "turbo": "^2.9.12",
    "typescript": "^6.0.3"
  }
}
